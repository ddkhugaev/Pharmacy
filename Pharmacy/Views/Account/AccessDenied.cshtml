@{
    ViewData["Title"] = "Доступ запрещён";
}

<div class="auth-wrapper">
    <div class="auth-card" style="text-align: center">
        <div style="font-size: 48px; margin-bottom: 16px">🚫</div>
        <h1 class="auth-title">Доступ запрещён</h1>
        <p class="auth-sub">У вас нет прав для просмотра этой страницы.</p>
        <a class="btn-green" asp-controller="Home" asp-action="Index">На главную</a>
    </div>
</div>
